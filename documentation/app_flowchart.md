flowchart TD
    A[User logs in through Clerk] --> B[Navigate to Upload Page]
    B --> C[Request secure upload URL via Next API route]
    C --> D[Upload video to Supabase Storage]
    D --> E[Call Next API route to trigger analysis]
    E --> F[External Python analysis service processes video]
    F --> G[Insert analysis results into Supabase PostgreSQL]
    G --> H[User navigates to Dashboard]
    H --> I[Fetch analysis results via React Query]
    I --> J[Display graphs and tables]
    H --> K[User opens RAG chat]
    K --> L[Call Next API route for RAG chat]
    L --> M[Query Supabase Vector DB via LangChain]
    M --> N[Return coaching tips]
    N --> O[Display tips in chat UI]